import React from 'react'; 

const Header = React.createClass({
   displayName: 'Header', 
    
    render() {
        return (
          <div>
           Header
          </div>
        );
    }
})

export default Header;
